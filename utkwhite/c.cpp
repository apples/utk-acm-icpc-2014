#include <iostream>
#include <vector>
#include <algorithm>
#include <map>
#include <cmath>
#include <fstream>
#include <sstream>

using namespace std;
int main(){
ifstream in;
in.open("lex.in");
string str;
long long k;
while(in>>str){
in>>k;
if(str=="#"&&k==0)break;
sort(str.begin(),str.end());


}


}